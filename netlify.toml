[build]
publish = "public"
command = "hugo --gc --minify && git submodule update --init --recursive"

[context.production.environment]
HUGO_VERSION = "0.140.2"  # Use your Hugo version
HUGO_ENV = "production"
HUGO_ENABLEGITINFO = "true"